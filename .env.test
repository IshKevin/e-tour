# Test Environment Configuration
NODE_ENV=test
PORT=3001

# Database Configuration (Test Database)
DATABASE_URL=postgresql://postgres:password@localhost:5432/etour_test

# JWT Configuration
JWT_SECRET=test-jwt-secret-key-for-testing-only
JWT_EXPIRES_IN=1h

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Email Configuration (Mocked in tests)
SMTP_HOST=smtp.test.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=test@example.com
SMTP_PASS=test-password
FROM_EMAIL=noreply@etour-test.com

# Cloudinary Configuration (Mocked in tests)
CLOUDINARY_CLOUD_NAME=test-cloud
CLOUDINARY_API_KEY=test-api-key
CLOUDINARY_API_SECRET=test-api-secret

# Test-specific settings
TEST_USER_EMAIL=testuser@example.com
TEST_USER_PASSWORD=testpassword123
TEST_AGENT_EMAIL=testagent@example.com
TEST_ADMIN_EMAIL=testadmin@example.com
